export const user = state => state.user;
export const userAuthenticated = state => state.user.authenticated;
export const authToken = state => state.user.token;
export const userRole = state => state.user.role;

export const stocks = state => state.stocks;
